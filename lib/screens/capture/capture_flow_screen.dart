import 'package:flutter/material.dart';

class CaptureFlowScreen extends StatelessWidget {
  const CaptureFlowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Capture Flow Screen')),
    );
  }
}
